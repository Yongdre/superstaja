# 리그 데이터 JSON 편집 가이드

## 가장 쉬운 사용법

1. 메인 메뉴에서 `새 시즌 시작`을 누릅니다.
2. `최신 입력 템플릿`을 눌러 10팀 × 타자 9명 × 투수 10명의 완전한 JSON 템플릿을 받습니다.
3. 원본을 복사해 둔 뒤 텍스트 편집기에서 JSON을 수정합니다.
4. `수정한 JSON 가져오기`를 눌러 파일을 검증합니다.
5. 화면에 새 데이터셋 이름이 표시되면 커리어를 시작합니다.

가져온 리그 데이터는 세이브 JSON 안에도 포함되므로 커리어 도중 사라지지 않습니다.

사용자가 제시한 형식의 복사 가능한 선수 예시는 `src/data/examples/player-stat-profile-examples.json`에 있습니다.

완성 후 게임에서 불러오는 파일은 선수 조각만 따로 모은 파일이 아니라 `schemaVersion`, `label`, `sourceSeason`, `teams`를 모두 포함한 완전한 리그 JSON입니다. 제공된 `src/data/templates/kbo-league-data-template.json`에서 팀 ID와 바깥 구조는 유지하고 타자 성적과 투수 능력치를 채운 뒤 그 파일 전체를 가져오면 됩니다. 타자의 `contact`, `power`, `discipline`, `speed`는 예상 성적에서 자동 계산되며, 투수의 네 능력치는 직접 입력합니다.

## 프로젝트에서 연도별로 관리하기

내장 데이터 파일은 다음 폴더에 둡니다.

```text
src/data/seasons/
  kbo-league-data-2017.json
  kbo-league-data-2018.json
  kbo-league-data-2019.json
  kbo-league-data-2099.json
```

새 연도를 추가하는 순서는 다음과 같습니다.

1. 기존 JSON을 같은 폴더에 복사하고 파일명을 `kbo-league-data-YYYY.json`으로 바꿉니다.
2. JSON 안의 `sourceSeason`을 같은 연도로 바꿉니다.
3. `label`, 팀, 선수, 예상 성적을 수정합니다.
4. 개발 서버를 다시 시작합니다.

앱은 `kbo-league-data-*.json` 파일을 자동 등록합니다. 새 커리어에서는 데뷔 연도와 같은 내장 데이터가 자동 선택되고, 시즌이 넘어갈 때 다음 연도 파일이 있으면 자동 선택됩니다. 해당 연도 파일이 없으면 이전 시즌 데이터를 유지하며, 오프시즌 화면에서 별도의 JSON을 가져올 수도 있습니다.

## 최상위 구조

```json
{
  "schemaVersion": 1,
  "label": "2099 가상 KBO",
  "sourceSeason": 2099,
  "teams": {
    "KIA": {},
    "DOO": {},
    "LOT": {},
    "NC": {},
    "SK": {},
    "LG": {},
    "NEX": {},
    "HAN": {},
    "KT": {},
    "SAM": {}
  }
}
```

현재 일정 엔진은 위 10개 팀 ID를 사용하므로 ID 자체는 바꾸지 마세요. `name`, `shortName`, `city`는 자유롭게 바꿀 수 있습니다.

## 팀 구조

```json
{
  "id": "SAM",
  "name": "서울 코멧츠",
  "shortName": "코멧츠",
  "city": "서울",
  "primary": "#2040ff",
  "accent": "#ffffff",
  "strength": 75,
  "hitters": [],
  "pitchers": []
}
```

- `primary`, `accent`: UI에 사용하는 CSS 색상입니다.
- `strength`: 현재 경기 결과에 직접 적용되지 않는 값입니다. 팀 전력을 정하려면 선수들의 성적·능력치와 선수층에 반영해야 합니다. 이 숫자만 바꿔 승률을 정하는 방식은 아닙니다.
- `hitters`: 최소 9명이 필요합니다. 백업과 부상을 사용하려면 팀당 12–15명을 권장합니다.
- `pitchers`: 최소 한 명의 `SP`와 한 명의 `RP` 또는 `CP`가 필요합니다.

## 타자 입력 — 최소한 필요한 성적

```json
{
  "id": "SAM-2099-001",
  "name": "레나스",
  "position": "2B,3B,SS",
  "bats": "R",
  "availability": "INJURY",
  "statProfile": {
    "games": 100,
    "avg": 0.324,
    "homeRuns": 20,
    "eye": 6,
    "speed": 8
  }
}
```

- `id`: 모든 타자와 투수를 통틀어 중복되면 안 됩니다.
- `bats`: `L` 또는 `R`입니다.
- `position`: `C, 1B, 2B, 3B, SS, LF, CF, RF, DH`를 사용합니다. 멀티 포지션은 `2B,3B,SS`처럼 쉼표로 연결합니다. `OF`, `IF`, `UTIL`도 사용할 수 있습니다.
- `statProfile`의 기본 입력은 `games`, `avg`, `homeRuns`, `eye`, `speed` 다섯 가지입니다.
- `games`: 목표 선발 출장 수입니다. 정확한 최종 기록을 강제하지는 않지만 라인업 배분 기준으로 사용합니다.
- `eye`: 선구안입니다. 1–10 정수이며 5가 리그 평균입니다. 볼넷과 출루 능력에 영향을 줍니다.
- `speed`: 주루 속도입니다. 1–10 정수이며 5가 리그 평균입니다. 도루 시도와 성공 가능성에 영향을 줍니다.
- 자동 도루는 `speed` 7부터 활성화됩니다. 7/8/9/10의 최종 성공률은 각각 72%/78%/84%/90%이며, 정규시즌 최대 시도 수는 각각 30/40/50/60회입니다. 실제 시도 수는 출루와 주자 상황에 따라 이보다 적을 수 있습니다. 성공 판정에는 투수·포수 능력이나 추가 주루 보정이 적용되지 않습니다.
- `availability`: 생략하거나 `REGULAR`이면 시즌 전체에 출장과 휴식을 배분합니다. `INJURY`이면 목표 경기 수를 제외한 결장을 한 번의 연속 부상 구간으로 만듭니다.

최신 템플릿에서는 다음처럼 간단하게 작성하면 됩니다.

```json
"statProfile": {
  "games": 144,
  "avg": 0.370,
  "homeRuns": 30,
  "eye": 9,
  "speed": 3
}
```

기존 파일의 상세 성적 필드는 호환을 위해 계속 읽지만 새 템플릿에는 넣지 않습니다. 위 다섯 값으로 `contact`, `power`, `discipline`, 내부 주루 능력을 자동 계산합니다. 일부만 직접 조정하려면 다음처럼 사용할 수 있습니다.

```json
"ratingOverrides": {
  "speed": 92
}
```

## 144경기와 백업 선수 구성

한 팀은 매 경기 `C, 1B, 2B, 3B, SS, LF, CF, RF, DH` 아홉 자리를 채웁니다. 따라서 한 시즌에 필요한 타자 선발 출장 합계는 약 `9 × 144 = 1,296`경기입니다.

- 주전 유격수 100경기 + `2B,3B,SS` 멀티 백업 44경기처럼 결장분을 채울 수 있습니다.
- 멀티 백업의 `games`는 각 포지션별 수가 아니라 그 선수가 선발 라인업에 들어가는 전체 경기 수입니다.
- 포수처럼 대체하기 어려운 포지션은 주전과 백업의 출장 합계가 144경기 안팎이 되도록 준비하는 것이 좋습니다.
- 여러 선수의 포지션과 목표 경기가 충돌하면 엔진이 가능한 조합을 우선하고 일부 선수는 목표 경기에서 조금 벗어날 수 있습니다.
- 포지션상 9명을 구성할 수 없는 JSON은 가져오기 단계에서 거부됩니다.

## 투수 입력 — 0–100 능력치

```json
{
  "id": "SAM-2099-SP1",
  "name": "조정훈",
  "throws": "R",
  "role": "SP",
  "grade": "A",
  "stuff": 82,
  "movement": 78,
  "control": 76,
  "stamina": 88
}
```

- `throws`: `L` 또는 `R`입니다.
- `role`: 선발 `SP`, 불펜 `RP`, 마무리 `CP` 중 하나입니다.
- `grade`: 화면에 표시할 투수 등급입니다. `S`, `A`, `B`, `C`, `D`만 허용하며, 입력한 값이 있으면 우선 표시합니다. 기존 JSON처럼 생략하면 `(stuff + movement + control) / 3`으로 자동 계산합니다. 체력은 제외하며 선발·불펜·마무리에 같은 기준을 씁니다.
- 자동 등급 기준은 평균 85 이상 S, 78 이상 A, 70 이상 B, 60 이상 C, 60 미만 D입니다. 평균을 반올림하기 전에 구간을 판정합니다. 등급은 표시용이므로 경기 확률에 추가 보정을 주지 않습니다.
- JSON을 생성할 때는 능력치를 먼저 정하고 이 기준으로 `grade`도 함께 적습니다. 능력치를 나중에 직접 수정했다면 `grade`도 갱신하거나 삭제해 자동 계산을 사용하세요. 게임 화면에는 방어율을 표시하지 않습니다.
- `stuff`: 구위. 탈삼진과 약한 타구에 가장 크게 관여합니다.
- `movement`: 무브먼트. 장타 억제와 땅볼 유도에 관여합니다.
- `control`: 제구. 볼넷 허용에 가장 크게 관여합니다.
- `stamina`: 체력. 선발의 투구 한계와 교체 판단에 사용합니다.
- 네 값은 모두 `0–100`이며 현재 경기 확률식의 중립 기준은 약 `72`입니다. `50`은 평균이 아니라 확실히 약한 편이고, 주전급은 대략 `68–80`, 리그 최상급 장점은 `85–95` 정도를 권장합니다.

투수 배열에서 `SP`가 등장하는 순서가 1–5선발 순서입니다. 정규시즌에는 5명을 모두 사용하고 포스트시즌에는 앞의 4명만 선발로 나옵니다. 5번째 `SP`는 포스트시즌에서 중간 불펜 후보로 자동 전환됩니다.

마무리 최소 예시는 다음과 같습니다.

```json
{
  "id": "SAM-2099-CP1",
  "name": "오승환",
  "throws": "R",
  "role": "CP",
  "grade": "S",
  "stuff": 95,
  "movement": 91,
  "control": 88,
  "stamina": 52
}
```

RP와 CP는 긴 이닝을 던질 필요가 없으므로 `stamina`가 낮아도 약한 투수라는 뜻은 아닙니다. 중요한 불펜은 `stuff`, `movement`, `control`을 높게 주면 됩니다.

## 기존 능력치 방식

기존 투수 JSON도 계속 사용할 수 있습니다. `movement`가 없는 기존 파일은 `stuff`와 같은 값으로 자동 보완합니다. 기존의 투수 `statProfile`도 호환을 위해 읽을 수 있지만, 새 파일에는 `stuff/movement/control/stamina` 직접 입력을 권장합니다.

## 주의사항

### 모바일 메뉴와 루타 확인

- 모바일 하단의 여섯 번째 `저장 / 불러오기`에서 수동 저장 슬롯 3개, 세이브 JSON 내보내기·가져오기를 사용할 수 있습니다. 하단 메뉴가 저장 알림에 가려지지 않도록 표시합니다.
- `선수 기록`의 통산 기록에는 `루타`가 표시됩니다. 연도별 기록 표에도 `홈런` 다음에 `루타`가 있으며, 좁은 화면에서는 표를 좌우로 밀어서 볼 수 있습니다.
- 루타는 이미 저장된 안타·2루타·3루타·홈런으로 계산하므로 기존 세이브도 별도 변환이나 시즌 재시작 없이 표시됩니다.

### 도루 성공률

- 내 선수는 커리어 시작 화면에 입력한 도루 성공률을 매번 그대로 사용합니다. 75%라면 각 시도에서 0.75 확률로 성공하며 투수·포수·주루 능력, 이닝, 이전 성공·실패로 확률이 바뀌지 않습니다.
- 다른 선수는 위 `speed` 등급별 최종 성공률을 사용합니다. 출루·빈 베이스·아웃 수와 최대 시도 수는 시도 여부에만 영향을 주며, 시도한 도루의 성공률과는 별개입니다.
- 독립 판정이므로 100번 시도해 반드시 75번 성공하는 방식은 아닙니다. 연속 성공·실패도 가능하며 누적 성공률을 맞추는 보정은 없습니다.
- 기존 세이브의 `config.stealSuccess` 값도 그대로 사용합니다. 고정 seed를 사용하는 게임이므로 동일한 세이브에서 동일한 선택을 반복하면 같은 결과가 재현되며, 불러오기로 다시 추첨하지 않습니다.

### 경기 기록 표시와 희생타

- `볼넷(고의사구, 사구)`의 큰 숫자는 일반 볼넷 + 고의사구 + 몸에 맞는 공의 합계입니다. 예를 들어 `45 (3, 5)`는 합계 45개 중 고의사구 3개와 사구 5개라는 뜻입니다. 공식 용어로는 사사구 합계에 해당하지만 화면은 요청한 볼넷 표기를 사용합니다.
- 저장 데이터의 `bb`에는 이미 `ibb`가 포함됩니다. `hbp`는 별도 보존하고, 화면에서만 `bb + hbp`를 계산합니다. 세이브의 `bb`에 사구를 다시 더하지 마세요.
- 루타는 `안타 + 2루타 + 2 × 3루타 + 3 × 홈런`입니다. 볼넷·사구·도루·실책 출루는 더하지 않습니다.
- 일반적인 실책 출루는 타석·타수가 증가하며 안타는 증가하지 않습니다.
- 희생플라이는 0·1사에 3루 주자가 있을 때 선택할 수 있습니다. 타석·SF·타점이 증가하고 타수는 증가하지 않습니다. 출루율 분모에는 포함됩니다.
- 희생번트는 0·1사에 1루 또는 2루 주자가 있고 3루가 비어 있을 때 선택할 수 있습니다. 타자는 아웃되고 주자는 한 베이스 진루합니다. 타석·SH가 증가하며 타수와 출루율 분모에는 포함되지 않습니다. 스퀴즈는 아직 지원하지 않습니다.
- 버튼은 성공한 결과를 선택하는 방식입니다. 자동 타자도 6회 이후 동점·1점차, 무사, 장타력이 낮은 타자의 진루 상황에서 제한적으로 희생번트를 기록합니다.
- 포스트시즌 경기별 사용자 기록을 저장해 와일드카드·준플레이오프·플레이오프·한국시리즈를 나누어 표시합니다. 완료된 시즌의 기록은 다음 시즌에도 남습니다.
- 이전 세이브의 볼넷·루타는 기존 기록으로 계산할 수 있습니다. 하지만 과거 포스트시즌 합계만으로 시리즈별 기록은 복원할 수 없어 누락을 안내합니다. 새로 시작하는 시리즈부터 정확한 분할 기록이 쌓입니다.

### 입력값과 엔진 개선 방향

현재 능력치 변환의 한계, 재현한 문제, 간단 성적 입력 방식에 대한 제안은 `CODE_REVIEW_2026-09-07.md`에 정리했습니다. 그 문서의 향후 제안 필드는 아직 게임에 구현된 JSON 규격이 아닙니다.

### 기타

- JSON은 마지막 항목 뒤에 쉼표를 넣을 수 없습니다.
- 파일명의 연도와 `sourceSeason`을 동일하게 관리하는 것을 권장합니다.
- 선수 ID는 기록 저장 키로 사용되므로 커리어를 시작한 뒤에는 바꾸지 않는 것이 안전합니다.
- 현재 선수 노화, 신인 생성, 트레이드 기능은 없습니다. 연도별 JSON을 추가하거나 오프시즌에 새 JSON을 가져와 시즌 데이터를 교체할 수 있습니다.
- 기본 2017 데이터는 공식 기록 파일이 아니라 MVP용 근사 데이터입니다.
