# 슈퍼스타자 코드 분석 — 2026-09-07

검토 범위: React 화면과 CSS, 경기·타석·주루 엔진, 라인업과 일정, 정규시즌·커리어·포스트시즌 집계, JSON 가져오기와 저장, 기존 테스트. 아래의 미해결 항목은 이번 기록 기능 수정과 구분한 후속 작업 제안이다.

후속 반영(2026-09-08): 아래에서 지적한 모바일 저장/불러오기 메뉴 누락은 해결했다. 도루는 투수·포수·추가 주루 보정 없는 고정 성공률 판정으로 변경했고, 방어율 대신 JSON `grade` 또는 능력치 평균으로 투수 등급을 표시한다. 그 외 엔진 문제는 아래 분석 시점의 미해결 제안으로 남아 있다.

## 판단

한 선수의 타석 결과를 선택하고 독립적인 리그를 따라가는 게임의 기본 구조는 갖춰져 있다. 고정 seed, 순수 계산 함수, 정규시즌/포스트시즌 기록 분리, 커리어에 데이터셋을 포함하는 저장 방식은 유지할 가치가 있다. 10팀 144경기 일정과 상대별 16경기/홈·원정 균형도 테스트되어 있다.

다만 현재 엔진은 타석 결과를 먼저 뽑고 세부 주루를 붙이는 간소화 모델이다. 실제 타구 속도·방향·수비 범위를 계산하는 물리 모델이나, 원하는 시즌 성적을 반복 실행으로 맞춘 모델은 아니다. 따라서 수치 하나를 정교하게 입력한다고 해당 성적이 재현된다고 보장할 단계는 아니다. 우선순위는 **투수 운용과 기록 규칙 → 확률/입력값 일관성 → 반복 시뮬레이션 보정 → 추가 능력치**로 권한다.

## 이번에 반영한 사항

- 시즌·통산·연도별 기록에 루타, 희생플라이, 희생번트를 표시한다. 통산 합계는 누적 횟수로 먼저 더하고 AVG/OBP/SLG/OPS를 다시 계산한다.
- 기존 ‘4구’ 표시를 ‘볼넷(고의사구, 사구)’로 통일한다. 합계는 `bb + hbp`, 괄호는 `ibb, hbp`이며 IBB를 두 번 더하지 않는다. 리그 기록의 볼넷 순위도 이 합계 기준이다. 루타 순위도 추가했다.
- 유저에게 SF/SH 결과 버튼을 제공한다. 상황이 맞지 않으면 비활성화하며, 엔진에서도 잘못된 선택을 거부한다.
- SH는 타석만 늘고 타수/출루율 분모는 늘지 않는다. 3루가 비어 있고 0·1사일 때 1·2루 주자를 한 베이스씩 보낸다. 스퀴즈와 번트 실패는 이 버튼의 범위에 포함하지 않았다. 기존 결과 선택 방식에 맞춰 성공 결과를 선택한다.
- 자동 타자도 후반 접전의 무사 진루 상황에서 제한적으로 SH를 기록한다. 이는 전술의 간단한 근사이며 번트 시도 성공률을 모델링한 것은 아니다.
- 기존 OUT 분기의 SF는 유지하면서 같은 처리 함수를 공유한다.
- 포스트시즌 경기별 사용자 기록을 저장한다. 경기 중·경기 종료·선수 기록에서 시리즈별 누적을 표시하고 시즌 종료 시 커리어에 보존한다.
- 이전 세이브에는 새 SH 값을 0으로 채운다. 기존 포스트시즌에 경기별 기록이 없다면 합계는 보존하되 시리즈별 수치를 임의로 분배하지 않는다.

## 1. 실책과 희생타에 대한 답

**일반적인 실책 출루는 타수에 포함되는 것이 맞다.** 기존 ROE 분기의 AB 증가는 오류가 아니다. 안타는 늘지 않기 때문에 타율은 내려간다. 포수 타격방해나 희생타로 인정되는 특수 상황은 별개이며 현재 모델에는 없다. [공식 AB 설명](https://www.mlb.com/glossary/standard-stats/at-bat)

| 결과 | 타석 | 타수 | 출루율 분모 | 이번 처리 |
|---|---:|---:|---:|---|
| 일반 실책 출루 | +1 | +1 | +1 | 기존 유지 |
| 일반 볼넷 / 고의사구 / 몸에 맞는 공 | +1 | 0 | +1 | 내부 구분, 화면 합산 |
| 희생플라이 | +1 | 0 | +1 | 기존 자동 결과 + 직접 선택 |
| 희생번트 | +1 | 0 | 0 | 신규 추가 |
| 일반 진루 땅볼 | +1 | +1 | +1 | 기존 처리 |

희생플라이는 기존 코드에도 있었지만 ‘아웃 계열 → 뜬공 → 3루 주자/아웃 조건 → 추가 확률’을 통과해야 보여 발견하기 어려웠다. 희생번트는 이번 수정 전에는 통계 필드 자체가 없었다. [SF 설명](https://www.mlb.com/glossary/standard-stats/sacrifice-fly), [SH 설명](https://www.mlb.com/glossary/standard-stats/sacrifice-bunt)

호수비가 없다고 실책을 삭제할 필요는 없다. 현재의 안타/아웃 확률은 이미 평균적인 수비 결과가 섞인 최종 결과다. 호수비는 우선 일부 확정 아웃의 중계 묘사로 넣을 수 있다. 실제 수비 능력까지 확장한다면 ‘타구 발생 → 수비 성공/실패 → 주루’ 단계를 함께 설계해야 한다. 이미 정한 안타를 추가 호수비 확률로 다시 지우면 입력 타율을 이중으로 낮출 수 있다. 유저가 직접 선택한 결과의 보장 원칙도 먼저 정해야 한다.

## 2. 우선 개선해야 할 재현 가능한 문제

### P1 — 같은 투수로 재교체하면서 투구 수가 초기화된다

위치: [gameEngine.ts:189](</C:/Users/OWNER/Documents/ChatGPT/batting simulation/src/engine/gameEngine.ts:189>)의 `checkPitchingChange`.

불펜 후보에서 현재 투수와 이미 내려간 투수를 제외하지 않는다. 선택한 투수의 상태도 매번 투구 수·아웃·실점을 0으로 새로 만든다. 마무리를 선택할 때에는 같은 CP를 반복 선택할 수도 있다.

`node scripts/audit-simulation.mjs`로 내장 데이터의 seed 1–20, 각 첫 경기, 유저 OUT 선택 조건을 실행했다. **20경기 모두 동일 팀·동일 투수의 교체 중계가 반복**되었다. 이는 다른 팀/다른 로스터 전체에 대한 발생률 추정은 아니지만 현재 경로의 오류를 직접 보여준다.

제안: 경기별 투수 등판 목록과 투수별 상태를 저장하고, 현재/교체된 투수를 후보에서 제외한다. 불펜 소진 시 현 투수를 유지하는 규칙도 필요하다. 이 문제가 해결되기 전에는 체력 수치의 정밀 조정 효과가 제한된다.

### P1 — 투수 교체 기준이 실제 투구 아웃 수와 어긋난다

위치: [gameEngine.ts:194](</C:/Users/OWNER/Documents/ChatGPT/batting simulation/src/engine/gameEngine.ts:194>).

최소 이닝 판단은 투수가 잡은 아웃 대신 `(inning - 1) * 3 + (BOTTOM ? 3 : 0) + outs`를 쓴다. 따라서 같은 5회라도 초·말의 의미가 달라진다. `playOne`은 공수 전환 후 교체 판단을 하므로 방금 투구한 쪽이 아닌 다음 수비팀을 검사하는 경우도 있다. 부진 교체조차 최소 이닝 조건과 묶여 있다.

제안: 실제 `outsRecorded`, 투구 수, 현재 수비팀을 기준으로 검사하고, 이닝 도중 강판과 공수 전환 시 교체를 명시적으로 나눈다. 대량 실점 조기 강판은 최소 이닝과 독립시킨다.

### P1 — 승계주자와 투수 승패/세이브 계산이 단순하다

위치: [plateAppearance.ts:36](</C:/Users/OWNER/Documents/ChatGPT/batting simulation/src/engine/plateAppearance.ts:36>), [gameEngine.ts:358](</C:/Users/OWNER/Documents/ChatGPT/batting simulation/src/engine/gameEngine.ts:358>).

주자에 출루를 허용한 투수 ID가 없다. 득점하면 현재 투수의 `runsAllowed`만 늘어난다. 따라서 앞 투수가 내보낸 주자에 대한 책임을 구분할 수 없다. 승리/패전은 마지막 역전 시점의 투수 후보, 세이브는 종료 점수차·불펜 여부·승리투수와 다른지로 정한다. 선발 5이닝 요건, 등판 시 세이브 상황, 실제 투구 아웃 수, 승계주자 책임을 모두 반영하지는 않는다. 승리와 세이브가 한 투수에게 동시에 붙는 이전 문제만 해결된 상태다.

제안: `responsiblePitcherId`, 등판 시 점수·주자, 투수별 기록을 먼저 보존한다. 실점과 자책점을 구분하려면 실책이 없었다고 가정한 이닝 종료 시점까지 추가로 추적해야 한다. 시즌 투수 ERA를 보정하려면 이 기초 기록부터 필요하다.

### P1 — 모순된 성적과 확률 합계를 검증하지 않는다

위치: [leagueDataset.ts:16](</C:/Users/OWNER/Documents/ChatGPT/batting simulation/src/data/leagueDataset.ts:16>), [plateAppearance.ts:126](</C:/Users/OWNER/Documents/ChatGPT/batting simulation/src/engine/plateAppearance.ts:126>).

각 필드가 양수인지 등은 확인하지만 `HR ≤ H ≤ AB ≤ PA`, 장타 합계와 총 안타의 관계 등을 검사하지 않는다. 진단에서 `PA=100, AB=100, AVG=.200, HR=900`도 통과했다. 타석 결과 확률을 차례대로 더할 때 총합이 1을 넘는지도 검사하지 않아 앞쪽 결과가 뒤쪽 결과를 밀어낼 수 있다.

제안: 입력의 관계를 검증하고, 한 선수의 모든 결과 확률이 0–1이며 합계 1이 되는 공통 확률 계산 함수를 만든다. 심하게 모순된 입력은 몰래 보정하지 말고 어느 값이 충돌하는지 알려준다.

## 3. 수치와 성적 입력의 한계

### 서로 다른 능력치 변환 기준

타자 `statProfile` 경로에서는 AVG/HR/BB를 직접 확률로 변환하고, 투수의 능력치를 더하거나 빼서 조정한다. 따라서 타자 contact/power만 수동 변경해도 모든 타격 결과에 동일하게 반영되는 구조가 아니다. 다른 분기에서는 능력치만으로 확률을 만든다.

특히 [playerProfiles.ts:41](</C:/Users/OWNER/Documents/ChatGPT/batting simulation/src/engine/playerProfiles.ts:41>)의 기존 투수 `statProfile` 변환은 ERA 4.5를 대략 50점으로 놓는다. 반면 타자 성적 기반 대결의 중립 투수 기준은 72점이다. 지난번 직접 작성한 2017 투수 JSON의 수동 보정식도 이 함수와 별개다.

실제 같은 양현종 입력 `ERA 3.44, IP 193⅓, K 158, BB 45`를 기존 변환기에 넣으면 **75/62/73/92**가 나온다. 지난번 JSON의 **77/81/87/97**과 다르다. 후자가 실측으로 유일하게 결정된 정답이라는 의미도 아니다. 둘 다 설계한 변환식의 결과이며, 일관된 기준과 반복 검증이 필요하다.

### 0홈런도 홈런이 나온다

`chooseAutoOutcome`은 HR 확률에 최소 0.003, 2루타와 3루타에도 최솟값을 둔다. 그래서 명시적인 0을 ‘없는 성적’처럼 취급하는 효과가 있다. 성장 가능성을 의도한 것이 아니라면 명시적인 0과 미입력을 구분해야 한다.

재현: 타자 `144경기, AVG .300, eye 5, speed 5`, 주자 없음, 각 조건 100,000타석, seed 2017. 선발 교체·휴식·실제 타순 효과가 없는 **통제된 타석 실험**이며 완전한 시즌 검증은 아니다.

| 입력 HR | 투수 stuff/movement/control | 결과 AVG | 590타석 환산 HR |
|---:|---:|---:|---:|
| 0 | 72/72/72 | .301 | 1.73 |
| 30 | 50/50/50 | .338 | 32.44 |
| 30 | 72/72/72 | .297 | 29.38 |
| 30 | 95/95/95 | .258 | 26.64 |

평균급 투수 상대의 타율·홈런은 입력 근처로 나오는 출발점이 있다. 다만 투수 구성과 경기당 실제 타석 수가 바뀌면 시즌 합계도 바뀐다. 모든 투수와 타자에게 동시에 임의의 목표 성적을 주고 모두 정확하게 맞추는 것은 리그 합계가 서로 충돌할 수 있다.

## 4. 라인업·운영·저장의 추가 관찰

| 우선도 | 관찰 | 권장 수정 |
|---|---|---|
| P2 | `lineup.ts`는 건강한 선수 뒤에 부상 선수도 후보로 넣어 로스터가 부족하면 부상 구간에도 출전시킨다. | 포지션별 시즌 커버리지 검증과 대체 선수 정책. 부상자는 완전히 제외할지 명시. |
| P2 | JSON 검증은 모든 팀에 1,296 선발출장을 요구하지만 사용자 팀 라인업에서는 사용자 포지션을 비우고 8명을 뽑는다. | 유저 팀/포지션을 알고 검사하는 단계에서 1,152 출장 기준과 실제 포지션 충족을 검사. |
| P2 | 포스트시즌 라인업은 `day % 144`로 정규시즌 계획을 재사용한다. | 가을의 건강 상태와 주전 우선순위를 별도 관리. |
| P2 | 포스트시즌 1–4선발 순환은 존재하나 시리즈가 바뀌면 1선발부터 리셋된다. 휴식일/피로는 없다. | 시리즈 사이 휴식일과 투수별 최근 등판을 저장. |
| P2 | 땅볼 진루/병살 처리, 끝내기 때 득점·루타 확정이 간소하다. | 실제 아웃 순서와 득점 시점을 나타내는 플레이 이벤트로 기록 처리. 예: 1사 병살의 추가 주자 처리, 끝내기 비홈런의 필요 득점 이후 처리. |
| P2 | 선수가 기록을 갖지만 투수 시즌별 IP/K/BB/HR/ER 기록표는 없다. | 투수 목표 성적 보정을 시작하기 전에 계측 추가. |
| P2 | `stamina`는 현재 경기 교체에만 쓰이며 연투·시즌 피로는 없다. 낮은 speed의 자동 도루는 없다. | 투수 경기당 체력/연투 회복, 주자 도루 적극성/성공률을 분리. |
| P2 | 모바일 하단 메뉴는 `nav.slice(0, 5)`라 저장/불러오기 메뉴가 빠지고, 사이드바도 숨겨진다. | 휴대폰에서 수동 저장·내보내기에 도달하는 메뉴를 제공. |
| P2 | 자동저장은 전체 상태를 동기 localStorage에 쓰며 용량 오류를 처리하지 않고 ‘자동 저장됨’을 표시한다. | 저장 성공/실패를 실제 결과로 표시하고 백업 내보내기 제공. 큰 커리어는 IndexedDB 고려. |
| P2 | 매 선택마다 전체 state를 clone하며, 경기 생성 시 출전하지 않는 팀까지 10팀 라인업을 만든다. WeakMap 계획 캐시는 clone된 팀 객체를 재사용하지 못한다. | 리그 데이터와 동적 상태 분리, 2팀만 생성, 데이터 식별자/연도/포지션 기반 캐시. 실제 기기 성능은 별도 측정 필요. |
| P3 | `strength`는 현재 경기 결과 계산에서 사용되지 않는다. 좌우 타석/투구 손도 대결 확률에 반영되지 않는다. | 입력 가이드에 영향 여부를 명확히 표시하거나 추후 효과 추가. |
| P3 | 일정 제목이 ‘삼성 일정’으로 고정되어 있다. | 사용자 팀명에서 동적으로 생성. |
| P3 | package.json 버전은 latest지만 lockfile은 있다. | 의도한 버전 범위를 고정하고 lockfile을 계속 보존. |

`validateSave`는 형식 전체를 엄밀히 검증하는 스키마 검증기는 아니다. 이번에 타격 카운트 누락/유효성 처리는 보완했지만, 임의로 편집된 세이브의 모든 팀/선수 참조와 경기 상태까지 보장하지는 않는다.

## 5. 추천하는 성적 입력 방식

**입력은 원하는 성적, 내부 계산은 공통 확률, 능력치는 사람이 이해하기 위한 요약**으로 통일하는 방식이 적합하다. 사용자가 105명에게 4개 능력치를 직접 추측하게 하는 부담을 줄일 수 있다.

다음 표는 **향후 입력기 제안**이다. 현재 JSON에서 새 필드가 모두 지원된다는 뜻은 아니다.

| 구분 | 간단 입력 | 정밀도를 높이는 선택 입력 |
|---|---|---|
| 타자 | 경기 수, 타율, 홈런, 선구안 1–10, 주루 1–10 | 2루타, 3루타, 볼넷, 삼진, 도루/실패, 타석 또는 타수 |
| SP | 역할, ERA, 이닝 | 선발 경기 수, K, BB, 피홈런, 피안타 |
| RP | 역할, ERA, 이닝, 등판 수, 기용 우선순위 | K, BB, 피홈런, 피안타 |
| CP | 역할, ERA, 이닝 또는 등판 수 | K, BB, 피홈런, 피안타 |

ERA만 주면 여러 종류의 투수가 같은 값을 가질 수 있다. ERA 3.00의 탈삼진형과 땅볼형을 구별하려면 K/BB/HR 같은 보조 지표가 필요하다. 누락된 항목은 같은 연도·역할 평균으로 채우고 추정임을 표시한다. 실제 과거 기록으로 능력을 추정할 때는 작은 표본을 평균 쪽으로 보정하되, 사용자가 직접 정한 가상의 목표 성적은 같은 이유로 강제로 평균화하지 않도록 ‘실제 기록 기반’과 ‘가상 목표’ 입력의 뜻을 구분하는 것이 좋다.

추천 변환 절차:

1. 원본 입력 성적을 보존하고 경기당/타석당 비율로 정리한다. 희생타·사구 등의 누락값도 한 곳에서 처리한다.
2. 안타 종류·BB·HBP·K·일반 아웃의 일관된 기본 확률을 만든다. 투수 쪽은 BB 허용·K·피HR·인플레이 안타 억제를 구분한다.
3. 타자와 투수를 그 시즌 리그 평균에 상대적으로 비교해 확률을 조정한다. 양쪽 모두 ‘이미 최종 성적’인 수치를 중복 보정하지 않는다.
4. 서로 다른 seed의 여러 시즌을 자동 실행해 선수별 평균뿐 아니라 리그 AVG/ERA/HR/BB/K, 타석·이닝, 성적의 흔들림도 비교한다.
5. 입력과 차이가 크면 변환 매개변수를 오프라인에서 조정한다. 플레이 중 시즌 말에 억지로 안타/홈런을 배정하는 방식은 피한다.
6. 최종 JSON에는 원본 성적, 누락값 추정 근거, 변환 버전과 계산한 능력치를 함께 보존한다. 출전량과 능력은 분리해 부상으로 100경기만 뛴 강타자의 타격 능력을 깎지 않는다.

타자의 타점·득점, 투수의 승수·세이브는 팀 동료와 경기 상황에 크게 의존하므로 필수 능력치 입력으로 쓰기보다는 결과 확인용 목표로 두는 편이 맞다. RP 순위는 **기용 순서/중요 상황 우선도**로 사용하고 투수 실력 자체와 구분해야 한다.

이 작업의 바로 다음 단계는 변환기를 크게 만드는 것보다 위 P1 문제를 먼저 정리하고 투수 경기/시즌 기록을 추가하는 것이다. 그래야 ‘이 정도 기록을 입력하면 실제로 어느 정도가 나오는가’를 믿을 수 있는 방식으로 확인할 수 있다.

## 검증 및 실행

- 기존 48개 테스트에 희생타, 통합 볼넷/루타, 시리즈 분리·중복 방지, 시즌 이월, 구형 세이브, 기록 화면 테스트를 추가했다.
- 2026-09-08 최종 검증: 테스트 11개 파일의 61개 항목, TypeScript 검사, Vite 배포용 빌드가 모두 통과했다.
- 진단 스크립트는 `node scripts/audit-simulation.mjs`로 실행하며 JSON/세이브 파일을 변경하지 않는다. 결과는 현재 엔진의 문제를 관찰하는 출력이지 모든 현실성 조건을 만족했다는 판정이 아니다.
- 브라우저에서 희생번트를 선택해 타수 0·SH 1, 해당 중계와 시즌/통산 표시를 확인했다. 긴 기록 표는 가로 스크롤로 보도록 처리했다.
- 게임 코드와 화면이 바뀌었으므로 배포에는 새 dist가 필요하다. 리그 JSON만 다시 가져오는 것으로 이 기능들이 배포되지는 않는다.
