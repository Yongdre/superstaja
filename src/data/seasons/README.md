# 연도별 리그 데이터

리그 데이터 파일은 `kbo-league-data-YYYY.json` 형식으로 이 폴더에 둡니다.

- 파일을 추가한 뒤 개발 서버를 다시 시작합니다.
- JSON의 `sourceSeason`을 파일명의 연도와 맞춥니다.
- 앱은 이 폴더의 파일을 자동 등록합니다.
- 해당 연도 커리어 또는 다음 시즌을 시작할 때 같은 `sourceSeason`의 데이터가 자동 선택됩니다.
- 자세한 필드 설명은 프로젝트 루트의 `DATA_GUIDE.md`를 참고하세요.
